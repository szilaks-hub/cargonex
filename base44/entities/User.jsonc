{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user",
        "logistics_viewer"
      ],
      "default": "user",
      "description": "The role of the user in the app"
    }
  }
}